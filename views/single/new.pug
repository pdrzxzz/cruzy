extends ../layouts/layout

block content
    h1 Create new game 
    form(action="/single" method="POST" class="validated-form new-room-form" novalidate enctype="multipart/form-data")
        label(for="theme") Theme: 
        input(type="text" id="theme")
        button Create
    